import 'package:flutter_abbv/token.dart';

import 'base_widget.dart';

class TextField extends Widget {
  TextField(List<Token> properties, List<Widget> children)
      : super(properties, children);
}
